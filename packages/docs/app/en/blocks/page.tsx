import type { Metadata } from 'next';
import BlocksIndex from '../../blocks/page';

export const metadata: Metadata = {
  title: 'Newspaper Blocks',
  description: 'Complete editorial layouts across Chinese, English, and Japanese typography.',
};

export default BlocksIndex;

import type { Metadata } from 'next';
import DataJournalism from '../../../blocks/data-journalism/page';

export const metadata: Metadata = {
  title: 'The City Observer · Data Edition',
  description: 'A complete editorial layout with bar, line and pie charts, annotations and sources.',
};

export default DataJournalism;
